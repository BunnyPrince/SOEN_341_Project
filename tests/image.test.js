// const proceed = require('../public/scripts/images');

describe('upload and edit Test', () => {
  it('should return a boolean', () =>{
    /*
    const value = showPreviewOne(event)
    expect(value).toEqual(true)
     */
    expect(true).toEqual(true)
  })
})
